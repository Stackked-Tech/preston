import TaskBoard from "@/components/hospitality/TaskBoard";

export const metadata = {
  title: "Maintenance Tasks | WHB Command Center",
};

export default function TasksPage() {
  return <TaskBoard />;
}
